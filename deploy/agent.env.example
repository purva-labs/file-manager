# Address owned by this node on your private network (Tailscale recommended).
PRIVATE_IP=100.64.0.11
AGENT_PORT=3091

# Use an absolute host path to a root-readable token file.
AGENT_TOKEN_FILE=/etc/file-manager-agent/token
